-- Rush OS — development seed data.
-- Runs after migrations on `supabase db reset`. Safe, fake data only.
-- Real schema and seed rows are added in Phase 1. This file is intentionally
-- empty for now so `supabase db reset` succeeds on a fresh setup.

-- Example (added later):
-- insert into suppliers (name) values ('Awal Dairy'), ('Origin Roasters');
